= link_to '<< back', :action => 'index'
.title
	= @host.name
#chart
%ul.host-detail
	- @host.ping_records.each do |p|
		%li
			%span.ms
				= "#{p.response_ms} ms"
			= "(##{p.response_code})"
			.time
				= "#{p.created_at.strftime "%b %d, %a %H:%M:%S"}"
:javascript
	draw(#{@pings_json});
