%h1
	NEW HOST

= form_for @host, :url => { :action => 'create' } do |f|
	%ul.list
		%li
			= f.label :name, 'name'
			= f.text_field :name
		%li
			= f.label :url, 'url'
			= f.text_field :url
		%li
			= f.submit 'create'